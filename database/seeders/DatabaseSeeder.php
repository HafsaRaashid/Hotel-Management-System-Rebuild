<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * Foundation-only: seeds no domain data. A backlog item introduces its own
 * seeder (or factory) alongside the entity/migration it adds.
 */
class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        //
    }
}
